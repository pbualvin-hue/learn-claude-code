# DECISIONS.md — 決策與攻防紀錄（即時追加，不回頭改寫）

## 優先序聲明：內容正確 ＞ 維護成本 ＞ 花費（使用者，2026-07-11）

## 2026-07-11 訪談定案
- 定位：先自用後開放親友（含非工程師）；長期維護。
- 內容範圍：官方功能 →（次）版本更新追蹤 →（後）實戰情境與自訂化教學。
- 更新機制：自動草稿＋人工審核（使用者選定，Recommended 採納）。
- 部署隱私：全公開，個人設定去識別化（使用者選定）。
- 預算：$0 增支（現有訂閱＋免費 hosting）。
- 使用情境：課程＋可查閱雙形態。

## 2026-07-11 第 1 輪：架構與技術選型
- 攻擊：Starlight 是鎖定點，換掉＝重寫？｜判定：駁回——內容全為 MD/MDX 與框架解耦（Non-Negotiable #6），重寫僅及版面層。
- 攻擊：更新管線鎖死在平台排程功能，功能變動即癱瘓｜判定：成立（修復成本：低）→ 修入 CLAUDE.md #9：管線核心是可手動觸發的 skill，排程只是觸發器。
- 攻擊：YAGNI——i18n／CMS／資料庫｜判定：不採用即無此問題，PLAN「明確不做」列管。

## 2026-07-11 第 2 輪：營運隱患
- 攻擊：依賴（Astro 等）誰更新？｜判定：成立（低）→ 靜態站無伺服器攻擊面，策略「build 壞了才升級」＋lockfile，寫入維護慣例。
- 攻擊：排程靜默失敗，內容悄悄過時｜判定：成立（低）→ 修入 CLAUDE.md 四：footer 顯示基準版本＋日期，落後可肉眼發現；PLAN Phase 3 完成標準納入。
- 攻擊：draft PR 堆積沒人審｜判定：成立（低）→ 排程訊息附提醒；停損閘門涵蓋（連續 3 次大改即降級）。

## 2026-07-11 第 3 輪：資料品質（優先序第一位，重點輪）
- 攻擊：LLM 草稿幻覺→公開錯誤教學，且審核者未必能辨識｜判定：成立（中，提供選項後使用者未加碼，採低成本組合）→ 修入：每功能描述附官方來源（#4）、未驗證行為強制標示（#4）、merge 前隨機抽 3 條對照官方文件（四、黃金抽查）。
- 攻擊：Pagefind 中文斷詞弱｜判定：成立（低）→ Phase 0 實測＋指令名保留英文＋情境索引頁補償；列入已知限制。
- 攻擊：官方改行為但舊頁沒跟著改（過時比錯誤更隱蔽）｜判定：成立（低）→ 每頁 base_version（#4）＋管線草稿須列「受影響參考頁清單」（Phase 3）。

## 2026-07-11 第 4 輪：安全隱私與最弱使用者
- 攻擊：個人 config 教學洩露環境資訊｜判定：成立（低）→ 修入 #5 去識別化檢查清單（無使用者名路徑／email／私人專案名／硬體細節）。
- 攻擊：網站本身的安全面｜判定：駁回——純靜態、無使用者輸入、無 secrets（Pages 用內建 GITHUB_TOKEN），全域安全底線觸發範圍未命中。
- 攻擊：非工程師照課程走會卡在終端機安裝步驟｜判定：成立（低）→ Phase 2 定量驗收：真人親友 30 分鐘實走不求助，失敗處回修。
- 衝突攤牌：GitHub Free 的 Pages 僅支援 public repo（查證 2026-07-11），與全域「新 repo 預設 private」衝突 → 提交使用者裁決（規劃凍結前確認），暫定 public（內容本即公開）。

## 2026-07-11 第 5 輪：成本與擴展（收斂輪）
- 攻擊：免費層是善意不是合約｜判定：成立（低）→ 成本表逐項標「非合約保證」＋風險表列替代（Cloudflare Pages/Netlify）＋停損閘門「被收回不自動掏錢」。
- 攻擊：排程吃訂閱額度失控｜判定：成立（低）→ 鎖定週 1、skill 限定增量讀取。
- 攻擊：擴展臨界點｜判定：駁回——靜態站對讀者數天然免疫；內容量 >60 頁再重整 IA（路線圖已列）。
- 本輪無正確性級新攻擊成立於未修復狀態 → **收斂，停止迭代**。

## 2026-07-11 第 6 輪：學習功能迭代（UX 與最弱使用者為主戰場）
使用者要求：以學習者角度補「融會貫通」所需功能，並以良好 UX 設計。新增功能：決策比較頁、心智模型圖、跟著做任務、練習 repo、疑難排解、glossary、quiz、進度勾選、cheatsheet、首頁分流。逐一攻擊：
- 攻擊：心智模型圖若是點陣圖——改版難維護、深色模式反色壞掉、手機看不清｜判定：成立（低）→ 修入 CLAUDE.md UX 規則：圖一律 Mermaid/SVG 文字源碼，可 diff、主題自適應。
- 攻擊：troubleshooting 用術語當標題，非工程師用症狀搜尋搜不到｜判定：成立（低）→ 修入：症狀式人話標題，術語放內文。
- 攻擊：「跟著做」含 shell 指令時 Windows/macOS 語法不同，親友照抄失敗｜判定：成立（低）→ 修入：練習以對話內操作為主，用到 shell 時雙平台標注。
- 攻擊：決策比較頁是最高學習價值也最易過時的頁（功能重疊關係隨版本變），過時的比較表會教錯選型｜判定：成立（低）→ 修入：列入更新管線強制複審清單（changelog 涉及 hooks/skills/agents/settings/權限即觸發）。
- 攻擊：練習 repo 的逐鍵步驟隨版本失效｜判定：成立（低）→ 修入：步驟寫目標導向，repo 標 base_version；留在 Phase 4 由停損閘門把關。
- 攻擊：localStorage 進度換裝置消失，親友以為網站壞了｜判定：成立（低）→ 修入：UI 明示「進度僅存於這台裝置」；帳號系統維持不做（維護成本＋隱私）。
- 攻擊：頁面死路——讀完不知道下一步，最弱使用者直接流失｜判定：成立（低）→ 修入：無死路原則（先備知識＋下一步＋搜尋無結果導向情境索引），Phase 2 驗收加無死路抽查。
- 攻擊：新手與查閱者兩種讀者混在同一首頁，互相干擾｜判定：成立（低）→ 修入：首頁雙入口分流。
- 攻擊：quiz／進度勾選是否 YAGNI——沒人走課程就沒人考試｜判定：部分成立 → 留 Phase 4，進入條件加「至少一位親友走完入門篇」。
- 本輪成立攻擊全數低成本修入，無未解正確性級攻擊 → 收斂。
文件變更：CLAUDE.md 1.0→1.1（新增學習體驗規則節）、PLAN.md 1.0→1.1（Phase 2/3/4 擴充，Phase 數與停損閘門不變）。

## 2026-07-11 第 7 輪：個人化起步（使用者提案：引導讀者做出自己的 CLAUDE.md）
提案動機：補「融會貫通最後一哩」——在自己的 CLAUDE.md 上寫自己的習慣＝把罐頭練習接到真實應用。逐一攻擊：
- 攻擊：給新手大而全模板照抄——規則沒對應真實問題就是噪音，膨脹後反傷體驗｜判定：成立（低）→ 修入 CLAUDE.md 個人化起步原則：三行起步、規則對應真實問題、兩次原則升格、定期修剪；產生器輸出上限 10 條。
- 攻擊：非工程師不寫程式，CLAUDE.md 課對他們無意義？｜判定：駁回反轉——非工程師更需要（語言、稱呼、輸出格式偏好），但問題組不同 → 雙讀者版本（開發者版／非工程師版）。
- 攻擊：單獨一課上完就忘，個人化不會變成習慣｜判定：成立（低）→ 修入：每課「跟著做」結尾固定問「這功能有沒有該進你 CLAUDE.md 的東西？」，個人化貫穿全課程。
- 攻擊：互動產生器是前端元件，違反「內容 MD 化」解耦原則？｜判定：部分成立 → 產生器歸類為工具元件非內容，輸出是純文字；不傳送不儲存輸入（隱私）；排 Phase 4（中成本，先驗證課程本身有人走完）。
- 時序判定：課程放 Phase 2 日常使用篇（使用者原話「讓他們起頭以後，再深入了解」——個人化要早，不是進階獎勵）；產生器留 Phase 4。
- 本輪成立攻擊全數低成本修入 → 收斂。
文件變更：CLAUDE.md 1.1→1.2、PLAN.md 1.1→1.2（Phase 2 加課程與個人化驗收、Phase 4 加產生器）。

## 2026-07-11 第 8 輪：專案驅動＋誠實教學＋信任教育（使用者提案）
提案內容：引導讀者用 Claude 做出第一個專案，互動中發現問題並沉澱 CLAUDE.md／skill／MCP 等；正面處理初學者的資安與完整性恐懼；教學誠實分析不誇大、缺陷更要講；教讀者怎麼跟 Claude 互動。逐一攻擊：
- 攻擊：「直至理想專案完成」無法成為網站承諾——每人理想專案不同，教材無法保姆任意專案｜判定：成立（低）→ 修正：範本專案軌道為載體，教可遷移的循環（遇到問題→怎麼問→發現功能→沉澱規則）；軌道終點附「下一步地圖」；承諾邊界誠實標示（Non-Negotiable #10 一部分）。
- 攻擊：自建 skill／MCP／外掛對非工程師過深，塞進同一軌道會嚇跑人｜判定：成立（低）→ 分軌：非工程師軌道止於 CLAUDE.md＋安全使用現成連接器（Phase 2）；自建功能為開發者軌道（Phase 4）。
- 攻擊：資安教學寫錯比不寫更糟（錯誤的安全感）｜判定：成立（低）→ trust/ 內容列入審核必抽清單，不進隨機池；Phase 2 驗收加「資安頁全數人工複核」。
- 攻擊：誠實寫缺點會勸退使用者｜判定：駁回——缺陷正是初學者在意的點，誠實是信任來源，符合第一優先序；升格 Non-Negotiable #10（每功能頁必含「限制與注意」節）。
- 攻擊：Phase 2 範圍膨脹（軌道＋trust＋互動課全擠進來）｜判定：部分成立 → 垂直切片守住：Phase 2 只做一條非工程師軌道與 trust 首批頁；第二條軌道與進階信任議題留 Phase 4；接受 Phase 2 工期變長，不拆新 Phase（閘門結構不動）。
- 本輪成立攻擊全數低成本修入 → 收斂。
文件變更：CLAUDE.md 1.2→1.3（新增 #10 誠實教學、#11 專案驅動主軸）、PLAN.md 1.2→1.3（learn/ 重定向、新增 trust/、「怎麼跟 Claude 說話」課、Phase 2 驗收三項新增、Phase 4 改開發者軌道）。

## 2026-07-12 裁決與凍結
- 使用者裁決：repo 設 public ✔（CLAUDE.md #8 生效）→ 四份文件＋SPEC.md 齊備，**規劃凍結**。

## 2026-07-12 第 9 輪：隱患盤點與細節標定（使用者要求：不留任何假設）
盤出 14 項未標定細節，全數說死於 SPEC.md v1.0；其中 3 項為成立的真隱患：
- 隱患：GitHub Pages 子路徑部署的 base path 坑（markdown 連結不自動加前綴、CSS 斷裂、index.mdx hero 忽略 base）——外部查證證實為常見故障｜判定：成立（低）→ SPEC #1 標定連結規則＋Phase 0 部署驗證加「子路徑連結與 CSS 全通」。
- 隱患：停損閘門依賴「親友使用人數」但全案沒有任何量測機制（不裝追蹤器）｜判定：成立（低）→ SPEC #10：第 4 週排程提醒人工檢查點，直接詢問＋自評＋issue/star 輔助。
- 隱患：公開站缺「非官方免責聲明」與內容授權條款——被誤認官方或內容被搬運時無依據｜判定：成立（低）→ SPEC #11：footer 免責聲明＋CC BY-NC-SA 4.0／MIT。
其餘標定要點：frontmatter 以 zod schema 強制（決定論優先）；範本專案定案「整理練習資料夾」（附自產 zip，不碰真實檔案）；參考頁定稿 14 頁；管線契約含狀態檔與 PR 檢查清單；圖不做 client-side 渲染；唯一待填變數＝GitHub 帳號名。
- 本輪無未解正確性攻擊 → 收斂。文件變更：新增 SPEC.md v1.0、PLAN.md 1.3→1.4、KICKOFF.md 閱讀順序加入 SPEC.md。

## 2026-07-12 第 10 輪：SPEC 待定項使用者裁決（工作方式修正後補問）
使用者回饋：細節錨定時能問的就問，不要自訂慣例預設迴避提問（已入個人記憶）。回頭將 SPEC 中自行擬定的四項改為詢問，裁決結果：
- GitHub 帳號：`pbualvin-hue` → SPEC #0/#1 回填，無待填變數。
- 內容授權：保留所有權利（推翻我擬的 CC BY-NC-SA／MIT 預設）→ SPEC #11。
- 範本專案：雙範本並行、讀者自選（推翻第 9 輪單軌道定案）→ SPEC #8；知情成本：Phase 2 工作量約 +60%。
- 排程時間：每週日 21:00（Asia/Taipei）→ SPEC #7。
文件變更：SPEC 1.0→1.1、PLAN 1.4→1.5。

## 翻案紀錄（補充）
- 範本專案「文件批次摘要」：第 9 輪駁回（隱私與版權變數多）→ 2026-07-12 使用者裁決納入為 B 軌道；緩解措施改為「預設自產範例文件包＋自備文件前強制過 trust/ 課程」——依據：使用者選擇權優先於我方風險預設，風險以設計緩解而非排除選項。
- 內容授權：我擬 CC BY-NC-SA 4.0 → 使用者裁決保留所有權利——依據：授權是權利人本人的決定，不適用慣例預設。

## 已駁回替代案總表（防重提）
| 替代案 | 駁回理由 |
|---|---|
| 全文翻譯官方文件 | 著作權風險，公開站不可行 |
| 全自動發布 | 錯誤教學直接公開，違反第一優先序 |
| Docusaurus / 自架後端 | 維護成本與花費高於需求 |
| 即時跟版 | 額度與維護成本不成比例，週更足夠 |
| 單頁 Artifact（S 規模） | 使用者選定公開分享＋自動更新，S 承載不了 |
| 帳號系統／雲端進度同步 | 維護成本與隱私不成比例，localStorage 已滿足（第 6 輪） |
| 點陣圖架構圖 | 難維護、深色模式壞、手機不可縮放——一律 Mermaid/SVG（第 6 輪） |

## 2026-07-13 第 11 輪：Anthropic Academy 納入（使用者提案）
提案：Anthropic 推出免費課程與證照（Anthropic Academy，anthropic.skilljar.com，約 20 門課全英文），參閱其內容納入學習網站（只要內容，不發證書）。查證與攻防：
- 授權查證（2026-07-13，兩路 web-collector）：Skilljar 課程內容**無任何明文授權**＝視同保留所有權利，不可翻譯／改作／摘要轉發布；GitHub anthropics/courses 為 CC BY-NC 4.0 但與本站「保留所有權利」混用有管理成本；AI Fluency 新聞稿稱 CC 授權但未指版本，不可依賴；商標無公開指引，footer 免責聲明（SPEC #11）已覆蓋。
- 攻擊：直接改作課程內容＝重蹈第 1 輪駁回的「全文翻譯官方文件」｜判定：成立 → 納入形態限縮為原創介紹＋外連（Non-Negotiable #1 不動）。
- 使用者裁決（2026-07-13）：**方案 A**——Phase 2 加「官方免費課程與證照」導覽頁（含課程對應表、英文與付費方案門檻誠實標注）＋官方課綱作本站學習路徑一次性涵蓋度檢查＋Academy 課程頁列入 sources。已駁回：翻譯改作課程內容（授權＋NN #1）；引用 anthropics/courses 範例（授權混用成本，範例自寫）。
- 文件變更：PLAN 1.5→1.6（Phase 2 實作項＋1）。

## 2026-07-13 Phase 0 執行發現（第 5 項排程實驗，過程紀錄）
- 發現：排程任務準時觸發（12:26 一次性任務，誤差 26 秒）且成功啟動新 session 執行任務 prompt，但**卡在第一個工具的權限提示**——排程 session 是全新對話，無任何預核准權限，而排程執行時使用者不在場，任務停滯等待核准（實測掛起約 1 小時）。
- 機制查證：排程工具明示「Run 中核准的工具權限會存在任務上，自動套用到後續 run」——即每個排程任務需先手動「Run now」跑一次並核准所需工具，之後的自動 run 才不會卡住。
- 對 Phase 3 的設計影響（先記錄，Phase 3 動工時落實）：/update-content 排程任務建立後必須先「Run now」預核准（git、gh、網路抓取）才算部署完成；此步驟寫入 Phase 3 完成標準。備選：專案 settings 允許清單涵蓋管線所需指令。
- Phase 0 第 5 項判定：機制可行性「觸發＋執行」已證實，「產出 draft PR」待權限預核准後補證（不擋 Phase 1-2，符合 PLAN 原定位）。

## 2026-07-13 Phase 0 第 5 項最終收尾（三次實驗總結）
三次一次性排程實驗，各揭露一道無人值守的啟動閘門：
1. **工具權限**（v1，12:26）：排程 session 無預核准權限，卡在第一個 Bash 提示掛起 ~1 小時。解法：專案 .claude/settings.json 窄範圍允許清單（已落地並版控）。
2. **資料夾信任**（v2，13:56 排定、14:26 實跑）：runner 以正斜線路徑查 ~/.claude.json 的 hasTrustDialogAccepted，與本 session 的反斜線紀錄是兩筆，查到 false 即跳過。解法：旗標已改 true（注意：App 設定回寫可能還原，排程靜默跳過時第一個檢查這裡；改前備份 ~/.claude.json.bak-20260713）。
3. **允許規則前綴比對**（v3，15:42）：⚠ 未完全驗證——v3 執行兩個 Bash 呼叫後靜默結束無產出，推斷 `curl -s https://...` 不匹配規則 `Bash(curl https://...*)`（前綴比對對旗標敏感）。已補 `-s`／`-sL` 變體規則。另發現：v2/v3 的 notifyOnCompletion 通知均未送達本 session，排程結果不能依賴通知，要主動查。
**最終判定**：觸發準時（三次皆準）、session 啟動與工具執行已證實；「無人值守全程跑完產出 draft PR」尚未一次成功，末段驗證併入 Phase 3 部署時以修正後允許清單重測（PLAN 原文：第 5 項失敗不擋 Phase 1-2）。對 Phase 3 完成標準的追加要求：部署排程任務後必須實測一次無人值守 run 全通，且 footer 日期停滯（PLAN 既有）之外，加上「排程靜默跳過」的人工檢查點（信任旗標＋允許清單比對）。

## 2026-07-13 Phase 1 執行發現（過程紀錄）
- **官方文件網域遷移（新發現，影響 Phase 3 管線）**：`docs.claude.com/en/docs/claude-code/*` 全數 301 永久轉址到 `code.claude.com/docs/en/*`（23 條 sources URL 一致，皆最終 200、內容相符）。判定：本 Phase sources 保留 `docs.claude.com` 形式（穩定入口、自動轉址、與 index.mdx 既有慣例一致），不擋驗收；但 Phase 3 更新管線抓 changelog／做連結檢查時需知此遷移，未來可評估 sources 標準化為 `code.claude.com`。changelog 本身仍在 `raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md`（未遷移，Phase 0 第 1 項不受影響）。
- **frontmatter 必填範圍決策**：content.config.ts 將 base_version/sources/audience/last_reviewed 設為 required 且**不設 optional 逃生口**（首頁 index.mdx 也照填），對齊 CLAUDE.md #4「每頁 frontmatter 必記」與 SPEC #4「決定論優先」——任何缺欄位的頁面直接 build fail，不靠審核記得檢查。屬 SPEC #4 的落實方式，非新決策。
- **多 agent 產文的連結 slug 風險**：5 個平行 docs-writer agent 各自猜相鄰頁 slug，出現 2 處把 settings-permissions 誤寫成 `../settings/`（死連結）。主對話以「grep 所有 `../xxx/` 目標比對 14 個實際 slug」集中稽核修正。列為已知風險：日後批次產文後，連結 slug 一律集中稽核一次，不依賴各 agent 自猜正確。
- **slash commands 官方頁現況（供 Phase 3 決策頁複審參考）**：官方 slash-commands 頁內容已朝 skills 傾斜（custom slash command 併入 skills 機制），內建指令另見 `commands` 頁。內容頁已據此撰寫；此為 hooks/skills/agents 重疊關係會隨版本變動的實例，正是決策比較頁（Phase 2）易過時、需列入更新管線強制複審的原因。

## 2026-07-13 Phase 2 執行發現（第一段：批次 0-2，過程紀錄）
- **Auto Mode 內容時效訊號（影響 Phase 3 管線＋settings 頁）**：撰寫軌道頁時 WebFetch 官方 permissions 頁，發現官方現已文件化較新的「Auto Mode」權限概念，但本站既有 `reference/settings-permissions.mdx`（base_version 2.1.207，Phase 1 已審）只寫 default/acceptEdits/plan/bypassPermissions 四種模式。判定：第一段軌道頁為與既有已發布頁一致，暫依舊四模式描述（非捏造，是一致性選擇）；但這是「官方改行為、舊頁未跟」的實例——Phase 3 更新管線複審 settings 頁時應評估補上 Auto Mode，並非本段擋點。
- **MDX 不支援 HTML 註解（authoring gotcha，回寫 agent brief 慣例）**：截圖 TODO 佔位符用 `<!-- -->` 會讓 MDX 解析失敗（`Unexpected character !`），須用 `{/* */}`。已修。日後給內容 agent 的 brief 一律指定 MDX 註解語法，不要用 HTML 註解。
- **agent 產出殘留 `</content>` 閉合標籤**：3/5 個 agent 在檔尾多寫一行 `</content>`，MDX 視為未匹配 JSX 閉合標籤 → build fail。已移除。列入批次後稽核固定項：新頁一律掃 `<!--`、`</content>`、簡繁混寫（本次抓到 getting-started「门」）。
- **連結稽核升級為腳本（取代 Phase 1 的人工 grep）**：新增 `scripts/check-links.mjs`——build 後掃 dist 所有 HTML 內部連結、相對於各頁 URL 解析、驗證目標實體存在，支援 `--allow-missing <前綴>` 白名單尚未產出的頁面。本段實測抓出唯一待補類別為 trust/（第二段產出），其餘 24 頁連結全通。此腳本為 DECISIONS「批次後集中稽核」的可執行化，後續每批次沿用。
- **跨段死連結處理**：第一段內容頁已指向第二段的 trust/what-not-to-share、trust/verify-ai-output（軌道 B 的硬閘門依賴此頁，SPEC #8）。為避免 live 站短暫出現 5 個死連結，判定：第一段不單獨 push 上線，push 延到第二段 trust/ 產出後一次部署（兩次審核檢查點、一次 deploy）。使用者已授權本 Phase 批次完成後自行 push；此為在授權範圍內的部署時機判斷。
- **心智模型圖落地方式（SPEC #9 具體化）**：手寫 `src/assets/diagrams/mental-model.svg` 為版控源碼，經 `src/components/MentalModel.astro` 以 `?raw` inline 進頁面（非 `<img>`）——inline 才能讓 SVG 內 `:root[data-theme]` 選擇器跟 Starlight 深淺色切換即時反色，全程零 client-side JS。實測淺/深色顏色正確翻轉、viewBox 響應式縮放。
- **驗證環境限制**：本 session 的 Browser pane 截圖工具逾時（renderer 卡住），改用 read_page（結構）＋javascript_tool（計算樣式）驗證，等效且更精確。視覺回歸若需截圖，留待可用環境補。

## 2026-07-14 Phase 2 第一波上線（骨架＋trust）與使用者回饋迭代
- **可讀性回饋（使用者，中老年受眾）→ 全站 CSS 迭代**：回饋「字太小、太文謅謅、都在看字、有壓力」。診斷（量測 DOM）發現真兇不是字級而是**字型堆疊無 CJK 字型**，中文在 Windows fallback 到又細又難讀的新細明體。修法：`src/styles/custom.css` 明確指定系統中文黑體（PingFang TC／微軟正黑／Noto Sans TC）＋放大字級（內文 18px、左側欄 17px、右側 ToC 13→15px）＋語氣改口語短句＋加視覺（h2 強調色條、emoji 標題錨點、Steps/CardGrid、圓潤提示框、行內 code 上色）。定案模板以 getting-started 為範本，套用全骨架 8 頁。**注意**：Browser pane 預覽環境無使用者的 Windows 系統字型，中文字型渲染只能由使用者在真實瀏覽器判斷（本 session 驗證限制）。
- **截圖策略裁決**：Claude Code 終端機截圖無法由主對話擷取，且「用網路免費圖當截圖」違反誠實教學（NN #10）＋著作權＋本站圖規則。使用者採方案 B：手繪 `terminal-startup.svg` 示意圖（明確標「示意圖，非真實截圖」，text-source、深淺色自適應），日後可由使用者補真實截圖替換。
- **trust/ 訓練預設值——內容正確裁決**：草稿曾斷言「Free/Pro/Max 預設用於訓練、保留 5 年」（來自條款更新＋報導交叉核對，非官方逐字）。使用者裁決**收斂為不斷言預設值**：改為「政策隨方案/時間變動、官方頁未明寫預設值，最可靠是自查設定頁」＋保留自查步驟與商業方案不訓練（有官方依據）。落實「內容正確 ＞ 一切」與 CLAUDE.md #4「僅靠推測一律不寫死」。
- **權限模式時效發現（呼應 Phase 1 Auto Mode 記錄，影響 Phase 3）**：官方 `permission-modes` 頁現含 `auto`（分類器）與 `dontAsk`（CI）等較新模式，本站 `reference/settings-permissions`（2.1.207）只列四種。trust 頁刻意只教四種給新手、細節連 reference。Phase 3 更新管線複審 settings/決策頁時應評估補新模式。
- **checkpointing/rewind 新發現**：官方 checkpointing 頁——`/rewind`（雙 Esc）為本機暫時還原，官方明講「不能取代版本控制」、且不追蹤 Bash 造成的檔案變動。已寫入 trust/git-as-undo 做對比。
- **連結稽核腳本驗證跨段依賴閉合**：trust/ 上線後，`node scripts/check-links.mjs`（無白名單）29 頁全通，證實骨架先前指向 trust/ 的 5 個跨段連結全部解掉。此腳本確立為每批次收尾固定關卡。
- **部署驗證**：push→Actions success→live 站 getting-started／first-project／trust／practice-folder.zip 皆 200、內容相符（補完 Phase 1 遺留的「公開網址可開」live 驗證）。

## 翻案紀錄
（無）
